import React from "react";

import { Container, Label } from "./styles";

const OpenCarTitle = () => {
  return (
    <Container>
      <Label>Tap to open the car</Label>
    </Container>
  );
};

export default OpenCarTitle;
